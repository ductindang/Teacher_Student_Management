@using BLL.DTOs
@{
    ViewData["Title"] = "Chỉnh sửa giáo viên";
    Layout = "~/Views/Shared/_LayoutProject.cshtml";
}

@model Teacher

<div>
    <!-- BREADCRUMB -->
    <div class="page-meta">
        <nav class="breadcrumb-style-one" aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Người dùng</a></li>
                <li class="breadcrumb-item" aria-current="page"><a href="@Url.Action("Index", "Teacher")">Quản lý giáo viên</a></li>
                <li class="breadcrumb-item active" aria-current="page">Chỉnh sửa giáo viên</li>
            </ol>
        </nav>
    </div>
    <!-- /BREADCRUMB -->

    <div class="layout-top-spacing col-12">
        <a href="@Url.Action("Index", "Teacher")" class="btn">
            Trở lại
        </a>
    </div>

    <div class="row layout-top-spacing">
        <div id="user-management" class="col-lg-12 layout-spacing">
            <div class="statbox widget box box-shadow">
                <div class="widget-header">
                    <div class="row">
                        <div class="col-xl-12 col-md-12 col-sm-12 col-12">
                            <h4>Chỉnh sửa</h4>
                        </div>
                    </div>
                </div>
                <div class="widget-content widget-content-area">
                    <div class="col-12">
                        <form asp-action="Edit" method="POST" id="general-settings">

                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group mb-4">
                                        <label for="fullname">Tên giáo viên <strong class="text-danger">*</strong></label>
                                        <input asp-for="FullName"
                                               type="text"
                                               id="fullname"
                                               class="form-control @(ViewData.ModelState["FullName"]?.Errors.Count > 0 ? "is-invalid" : "")"
                                               placeholder="Tên giáo viên">
                                        <span asp-validation-for="FullName" class="invalid-feedback" role="alert"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group mb-4">
                                        <label for="experience">Số năm kinh nghiệm <strong class="text-danger">*</strong></label>
                                        <input asp-for="Experience"
                                               type="number"
                                               min="1"
                                               id="experience"
                                               class="form-control @(ViewData.ModelState["Experience"]?.Errors.Count > 0 ? "is-invalid" : "")"
                                               placeholder="Nhập số năm kinh nghiệm">
                                        <span asp-validation-for="Experience" class="invalid-feedback"></span>
                                    </div>
                                </div>

                            </div>
                            <div class="row">
                                <div class="form-group mb-4">
                                    <label for="degree">Bằng cấp <strong class="text-danger">*</strong></label>
                                    <input asp-for="Degree"
                                           type="text"
                                           id="degree"
                                           class="form-control @(ViewData.ModelState["Degree"]?.Errors.Count > 0 ? "is-invalid" : "")"
                                           placeholder="Bằng cấp của giáo viên">
                                    <span asp-validation-for="Degree" class="invalid-feedback" role="alert"></span>
                                </div>
                            </div>


                            <button type="submit" class="btn btn-primary">Hoàn tất</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>